import type { FastifyPluginAsyncZod } from "fastify-type-provider-zod";

/** Authenticated customer routes (saved configurations, applications). */
export const customerRoutes: FastifyPluginAsyncZod = async (_server) => {
  // Routes registered here by feature agents.
};
